<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.name}" plugins-version="2">

    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
    </plugin-info>

    <rest key="rest-service-resources" path="/tribs-rest" version="1.0">
        <description>Provides the REST resource for the tribs plugin.</description>
    </rest>

    <component key="contributionsService"
        name="Contributions Service"
        class="au.id.wolfe.tribs.service.impl.ContributionsServiceImpl"/>
	
</atlassian-plugin>